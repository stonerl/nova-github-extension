{
  "name": "nova-github-extension",
  "version": "0.9.2",
  "private": true,
  "description": "Test harness for the GitHub Nova extension (the extension itself has no build step)",
  "scripts": {
    "test": "node --test --test-force-exit test/"
  }
}
